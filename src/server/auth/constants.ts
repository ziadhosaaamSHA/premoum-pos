export const SESSION_COOKIE_NAME = "pos_session";

export const SESSION_TTL_HOURS = Number(process.env.SESSION_TTL_HOURS || 24);
export const INVITE_TTL_HOURS = Number(process.env.INVITE_TTL_HOURS || 72);
